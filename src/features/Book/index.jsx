import React from 'react';
import PropTypes from 'prop-types';

Book.propTypes = {

};

function Book(props) {
    return (
        <div>
            Book
        </div>
    );
}

export default Book;